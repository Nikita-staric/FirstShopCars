@model Car

@{
    ViewData["Title"] = "Home Page";
}
<div class="col-lg-4">
    
    <img class="img-thubnail" src="@Model.Img" alt="@Model.Name" />
    <h2>@Model.Name</h2>
    <p>@Model.ShortDesc</p>
    <p>Price-></p>@Model.Price.ToString("c");
    <p><a class="btn btn-warning" asp-controller="ShopCart" asp-action="addToCarss" asp-route-id="@Model.Id" erhref="#">Добавить в корзину</a></p>
</div>
